{
  "name": "@arp/scheduler",
  "version": "0.1.0",
  "description": "Targon Nexus Scheduler — cron-based job orchestration for periodic crawling, extraction, and graph updates",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint \"src/**/*.ts\" --fix"
  },
  "dependencies": {
    "@arp/shared": "workspace:*",
    "@arp/types": "workspace:*",
    "bullmq": "^5.1.0",
    "ioredis": "^5.3.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@arp/eslint-config": "workspace:*",
    "@types/node": "^20.10.0",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
